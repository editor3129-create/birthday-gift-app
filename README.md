# Birthday Gift App — for `my sweetie fox`

A small, single-page web app you can open in a browser to wish your girlfriend a happy birthday. It uses a black & purple theme and simple animated hearts.

Files:
- `index.html` — main page
- `styles.css` — styling and animations
- `script.js` — interactivity (surprise toggle and heart animations)

How to run

- Quick: double-click `index.html` in the `birthday-gift-app` folder to open it in your default browser.

- Optional (serve from a simple HTTP server, useful if your browser restricts local files):

PowerShell (if you have Python installed):

```powershell
python -m http.server 8000
# then open http://localhost:8000 in your browser
```

Customization

- Edit the text in `index.html` to change the message.
- Modify colors in `styles.css` (`--accent`, `--bg1`, `--bg2`) to tweak the theme.
- Replace the heart emojis in `script.js` if you want different icons.

Enjoy — and happy birthday to your sweetie fox! 💜
